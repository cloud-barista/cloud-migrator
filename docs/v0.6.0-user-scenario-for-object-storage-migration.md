# Object Storage migration scenarios

> [!IMPORTANT]
> This document describes the **portal/upper-subsystem perspective** of object storage migration scenarios.
> The scenarios represent the interaction between the user, the web console (Butterfly), and backend subsystems (Beetle, Tumblebug) during the full object storage migration workflow.

> [!NOTE]
> Unlike computing infrastructure migration, object storage migration does **not** use an agent for source discovery.
> The user manually registers the source object storage information through the portal UI.

> [!NOTE]
> **Namespace (nsId):** All migration APIs require a namespace ID (`nsId`). The namespace is selected or created at the start of the workflow (typically shared with the computing infrastructure migration). All object storage resources created in this workflow are scoped to the selected `nsId`.

---

## Login

- Participants: Butterfly

> [!NOTE]
>
> - The user logs in to the Cloud-Migrator web console.
> - Key flow:
>   - --> User accesses the web console
>   - --> Login page is displayed
>   - --> User enters credentials and they are validated
>   - --> User is redirected to the dashboard

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant WebServer as "Butterfly"

    %% Step 1: Access the Cloud-Migrator Web Console
    User->>Browser: Access the Cloud-Migrator Web Console
    activate Browser
    Browser->>WebServer: Request a view to login
    activate WebServer
    WebServer-->>Browser: Return the view to login
    deactivate WebServer
    Browser-->>User: Display login page
    deactivate Browser

    %% Step 2: Enter login credentials
    User->>Browser: Enter login credentials
    activate Browser
    Browser->>WebServer: Send login credentials (Username, Password)
    activate WebServer
    WebServer-->>Browser: Validate and return the dashboard page
    deactivate WebServer
    Browser-->>User: Display dashboard page
    deactivate Browser
```

---

## Register source object storage information

- Participants: Butterfly, Damselfly

> [!NOTE]
>
> - The user manually registers information about the source object storage buckets.
> - No agent installation is required — the user directly inputs all bucket metadata through the portal UI.
> - The registered source bucket list is saved to Damselfly as a **source object storage model** for use in the recommendation step.
> - Key flow:
>   - --> User navigates to the Object Storage Migration view
>   - --> User fills in source bucket details (name, size, object count, access frequency, feature flags, tags, etc.)
>   - --> User adds multiple buckets to the source list if needed
>   - --> User saves the source bucket list to Damselfly as a source object storage model

> [!TIP]
>
> **Model specification (storage-model)**
> All input fields on the registration form are defined in the following model file.
> Refer to this before designing or implementing the registration UI.
>
> - [`imdl/storage-model/model.go`](https://github.com/cloud-barista/cm-beetle/blob/main/imdl/storage-model/model.go)
>   - `SourceObjectStorage`: source bucket fields (bucketName, totalSizeBytes, objectCount, accessFrequency, versioningEnabled, encryptionEnabled, corsEnabled, corsRule, isPublic, tags)
>   - `TargetObjectStorage`: target bucket fields (bucketName, sourceBucketName, versioningEnabled, corsEnabled, corsRule)
>   - `RecommendedObjectStorage`: recommendation result and direct input to the migration API (status, description, warnings, targetCloud, targetObjectStorages)
>   - `CloudProperty`: cloud provider and region pair (csp, region)

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant Butterfly as "Butterfly"
    participant Damselfly as "Damselfly"

    %% Step 0: User accesses the Object Storage Migration view
    User->>Browser: Access the Object Storage Migration view
    activate Browser
    Browser->>Butterfly: Request the Object Storage Migration view
    activate Butterfly
    Butterfly-->>Browser: Respond with the view
    deactivate Butterfly
    Browser-->>User: Display Object Storage Migration interface
    deactivate Browser

    %% Step 1: Register source bucket information
    User->>Browser: Fill in source bucket details
    Note over Browser,Butterfly: Input fields (SourceObjectStorage):<br/>bucketName (required), totalSizeBytes,<br/>objectCount, accessFrequency (frequent/infrequent/archive),<br/>versioningEnabled, encryptionEnabled,<br/>corsEnabled, corsRule, isPublic, tags
    activate Browser
    Browser-->>User: Reflect entered information in the form
    deactivate Browser

    %% Step 2: Add more buckets (optional)
    opt Add more source buckets
        User->>Browser: Add another source bucket and fill in details
        activate Browser
        Browser-->>User: Add bucket to the source list
        deactivate Browser
    end

    %% Step 3: Confirm source list
    User->>Browser: Confirm the source bucket list
    activate Browser
    Browser-->>User: Display source bucket list summary
    deactivate Browser

    %% Step 4: Save source object storage model to Damselfly
    User->>Browser: Click "Save Source Model"
    activate Browser
    Browser->>Butterfly: Request to save source object storage model
    activate Butterfly
    Butterfly->>Damselfly: API call POST /damselfly/sourceObjectStorageModel
    Note over Butterfly,Damselfly: Payload: list of SourceObjectStorage entries<br/>(bucketName, totalSizeBytes, objectCount,<br/>accessFrequency, versioningEnabled, encryptionEnabled,<br/>corsEnabled, corsRule, isPublic, tags)
    activate Damselfly
    Damselfly-->>Butterfly: Return saved source model info (model ID)
    deactivate Damselfly
    Butterfly-->>Browser: Respond with save confirmation
    deactivate Butterfly
    Browser-->>User: Display save confirmation
    deactivate Browser

    %% Optional: Modify and re-save the source model
    opt Modify and re-save the source model
        User->>Browser: Edit source bucket entries and click "Update Source Model"
        activate Browser
        Browser->>Butterfly: Request to update source object storage model
        activate Butterfly
        Butterfly->>Damselfly: API call PUT /damselfly/sourceObjectStorageModel
        activate Damselfly
        Damselfly-->>Butterfly: Return updated model info (version specified)
        deactivate Damselfly
        Butterfly-->>Browser: Respond with update confirmation
        deactivate Butterfly
        Browser-->>User: Display update confirmation
        deactivate Browser
    end
```

---

## Recommend target object storage

- Participants: Butterfly, Damselfly, Beetle, Tumblebug

> [!NOTE]
>
> - The view loads the saved source object storage models from Damselfly for selection.
> - The user selects the target CSP and region on the same view (provider/region lists are loaded from Tumblebug when the page opens).
> - Based on the selected source model and target cloud, Beetle recommends the target object storage configuration.
> - The recommendation result is saved to Damselfly as a **target object storage model**.
> - Key flow:
>   - --> Load source object storage model list from Damselfly in parallel with provider list from Tumblebug
>   - --> User selects a source model and a target CSP/region on the same form
>   - --> User clicks "Get Recommendation" → Beetle analyzes and returns target configuration
>   - --> Beetle performs feature-compatibility analysis and generates target bucket configurations
>   - --> Display recommendation result including target bucket names, feature settings, and warnings
>   - --> User reviews and optionally modifies the recommendation
>   - --> Save the recommendation result to Damselfly as a target object storage model

> [!TIP]
>
> **API request/response examples and test results**
> The following test result files contain real API request bodies, response bodies, and migration results for each cloud provider.
> Refer to these to understand the expected input/output of the recommendation and migration APIs.
>
> - [`cmd/test-cli/object-storage/testresult/`](https://github.com/cloud-barista/cm-beetle/tree/main/cmd/test-cli/object-storage/testresult)
>   - [AWS](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-aws.md)
>   - [Azure](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-azure.md)
>   - [GCP](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-gcp.md)
>   - [NCP](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-ncp.md)
>   - [Alibaba](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-alibaba.md)
>   - [IBM](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-ibm.md)
>   - [NHN](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-nhn.md)
>   - [KT](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-kt.md)
>   - [Tencent](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-tencent.md)
>   - [OpenStack](https://github.com/cloud-barista/cm-beetle/blob/main/cmd/test-cli/object-storage/testresult/os-test-results-openstack.md)

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant Butterfly as "Butterfly"
    participant Damselfly as "Damselfly"
    participant Beetle as "Beetle"
    participant Tumblebug as "Tumblebug"

    %% Step 0: Load view — source models (Damselfly) and provider list (Tumblebug) in parallel
    User->>Browser: Access the Recommendation view
    activate Browser
    Browser->>Butterfly: Request the Recommendation view
    activate Butterfly
    par Load source object storage models from Damselfly
        Butterfly->>Damselfly: API call GET /damselfly/sourceObjectStorageModel
        activate Damselfly
        Damselfly-->>Butterfly: Return source object storage model list
        deactivate Damselfly
    and Load cloud provider list from Tumblebug
        Butterfly->>Tumblebug: API call GET /tumblebug/provider
        activate Tumblebug
        Tumblebug-->>Butterfly: Return list of registered providers
        deactivate Tumblebug
    end
    Butterfly-->>Browser: Respond with the view
    deactivate Butterfly
    Browser-->>User: Display Recommendation interface (source model list + provider dropdown)
    deactivate Browser

    %% Step 0-1: User selects provider → load regions
    User->>Browser: Select a cloud provider (e.g., aws)
    activate Browser
    Browser->>Butterfly: Request regions for the selected provider
    activate Butterfly
    Butterfly->>Tumblebug: API call GET /tumblebug/provider/{providerName}/region
    activate Tumblebug
    Tumblebug-->>Butterfly: Return list of registered regions
    deactivate Tumblebug
    Butterfly-->>Browser: Respond with region list
    deactivate Butterfly
    Browser-->>User: Display region selection dropdown
    deactivate Browser

    %% Step 1: Select source model, target region, and request recommendation
    User->>Browser: Select a source model, select a region, and click "Get Recommendation"
    activate Browser
    Browser->>Butterfly: Request to recommend target object storage
    activate Butterfly
    Butterfly->>Beetle: API call POST /beetle/recommendation/middleware/objectStorage
    Note over Butterfly,Beetle: Request body (RecommendObjectStorageRequest):<br/>desiredCloud: { csp, region }<br/>sourceObjectStorages: [ { bucketName, totalSizeBytes,<br/>  objectCount, accessFrequency,<br/>  versioningEnabled, encryptionEnabled,<br/>  corsEnabled, corsRule, isPublic, tags } ]
    activate Beetle
    Beetle->>Beetle: Analyze source bucket features
    Beetle->>Tumblebug: API call GET /tumblebug/objectStorage/support?cspType={csp}
    activate Tumblebug
    Note over Beetle,Tumblebug: Query CSP feature support (versioning, CORS, etc.)
    Tumblebug-->>Beetle: Return ObjectStorageSupportResponse
    deactivate Tumblebug
    Beetle->>Beetle: Apply CSP compatibility (disable unsupported features, emit warnings)
    Beetle->>Beetle: Generate target bucket configurations
    Beetle-->>Butterfly: Return recommendation result (RecommendedObjectStorage)
    deactivate Beetle
    Butterfly-->>Browser: Respond with recommendation result
    deactivate Butterfly
    Browser-->>User: Display recommendation result
    Note over Browser,User: Displays:<br/>status (recommended/partial/failed)<br/>targetObjectStorages (bucket name, versioning, CORS)<br/>warnings (CSP feature gaps, e.g., "versioning disabled on NCP")<br/>description (human-readable summary)
    deactivate Browser

    %% Step 2: Review and modify recommendation (optional)
    opt Modify recommended target configuration
        User->>Browser: Edit target bucket names or feature settings
        activate Browser
        Browser-->>User: Update the form with modified values
        deactivate Browser
    end

    %% Step 3: Save target object storage model to Damselfly
    User->>Browser: Click "Save Target Model"
    activate Browser
    Browser->>Butterfly: Request to save target object storage model
    activate Butterfly
    Butterfly->>Damselfly: API call POST /damselfly/targetObjectStorageModel
    Note over Butterfly,Damselfly: Payload: RecommendedObjectStorage<br/>targetCloud: { csp, region }<br/>targetObjectStorages: [ { bucketName, sourceBucketName,<br/>  versioningEnabled, corsEnabled, corsRule } ]<br/>status, description, warnings
    activate Damselfly
    Damselfly-->>Butterfly: Return saved target model info (model ID)
    deactivate Damselfly
    Butterfly-->>Browser: Respond with save confirmation
    deactivate Butterfly
    Browser-->>User: Display save confirmation
    deactivate Browser

    %% Optional: Modify and re-save the target model
    opt Modify and re-save the target model
        User->>Browser: Edit target model entries and click "Update Target Model"
        activate Browser
        Browser->>Butterfly: Request to update target object storage model
        activate Butterfly
        Butterfly->>Damselfly: API call PUT /damselfly/targetObjectStorageModel
        activate Damselfly
        Damselfly-->>Butterfly: Return updated model info (version specified)
        deactivate Damselfly
        Butterfly-->>Browser: Respond with update confirmation
        deactivate Butterfly
        Browser-->>User: Display update confirmation
        deactivate Browser
    end
```

---

## Create (migrate) target object storage buckets

- Participants: Butterfly, Damselfly, Beetle, Tumblebug

> [!NOTE]
>
> - The view loads the saved target object storage models from Damselfly for selection.
> - The user proceeds with the selected (or modified) target model to create object storage buckets in the target cloud.
> - Beetle calls Tumblebug to create the actual cloud resources.
> - Key flow:
>   - --> Load target object storage model list from Damselfly
>   - --> User selects a target model and clicks "Create Object Storage"
>   - --> Beetle calls Tumblebug to create each bucket
>   - --> Migration result including bucket ID (osId) and status is returned
>   - --> The osId is retained for use in the data migration step

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant Butterfly as "Butterfly"
    participant Damselfly as "Damselfly"
    participant Beetle as "Beetle"
    participant Tumblebug as "Tumblebug"

    %% Step 0: Load view and retrieve target object storage models from Damselfly
    User->>Browser: Access the Object Storage Migration execution view
    activate Browser
    Browser->>Butterfly: Request the migration execution view
    activate Butterfly
    Butterfly->>Damselfly: API call GET /damselfly/targetObjectStorageModel
    activate Damselfly
    Damselfly-->>Butterfly: Return target object storage model list
    deactivate Damselfly
    Butterfly-->>Browser: Respond with the view
    deactivate Butterfly
    Browser-->>User: Display migration execution interface with target model list
    deactivate Browser

    %% Step 1: Execute object storage migration
    User->>Browser: Select a target model and click "Create Object Storage"
    activate Browser
    Browser->>Butterfly: Request to create target object storage
    activate Butterfly
    Butterfly->>Beetle: API call POST /beetle/migration/middleware/ns/{nsId}/objectStorage
    Note over Butterfly,Beetle: Request body (MigrateObjectStorageRequest = RecommendedObjectStorage):<br/>targetCloud: { csp, region }<br/>targetObjectStorages: [ { bucketName, sourceBucketName,<br/>  versioningEnabled, corsEnabled, corsRule } ]<br/>status, description, warnings
    activate Beetle

    loop For each target bucket
        Beetle->>Tumblebug: API call PUT /tumblebug/ns/{nsId}/resources/objectStorage
        Note over Beetle,Tumblebug: Create bucket<br/>{ bucketName, connectionName, description }
        activate Tumblebug
        Tumblebug-->>Beetle: Return created ObjectStorageInfo (id, name, status)
        deactivate Tumblebug
    end

    Beetle->>Tumblebug: API call GET /tumblebug/objectStorage/support?cspType={csp}
    activate Tumblebug
    Note over Beetle,Tumblebug: Re-check CSP feature support<br/>(versioning, CORS) after user may have modified recommendation
    Tumblebug-->>Beetle: Return ObjectStorageSupportResponse
    deactivate Tumblebug

    loop For each target bucket (feature configuration)
        opt versioningEnabled = true AND CSP supports versioning
            Beetle->>Tumblebug: API call PUT /tumblebug/ns/{nsId}/resources/objectStorage/{osId}/versioning
            activate Tumblebug
            Note over Beetle,Tumblebug: { status: "Enabled" }
            Tumblebug-->>Beetle: Return versioning result
            deactivate Tumblebug
        end
        opt corsEnabled = true AND CSP supports CORS
            Beetle->>Tumblebug: API call PUT /tumblebug/ns/{nsId}/resources/objectStorage/{osId}/cors
            activate Tumblebug
            Note over Beetle,Tumblebug: { corsRule: [ { allowedOrigin, allowedMethod,<br/>  allowedHeader, exposeHeader, maxAgeSeconds } ] }
            Tumblebug-->>Beetle: Return CORS result
            deactivate Tumblebug
        end
    end

    Beetle-->>Butterfly: Return migration result (MigratedObjectStorageInfo)
    deactivate Beetle
    Butterfly-->>Browser: Respond with migration result
    deactivate Butterfly
    Browser-->>User: Display migration result
    Note over Browser,User: Displays for each bucket:<br/>id (osId), name, connectionName,<br/>status (e.g., "Available"), creationDate, description
    deactivate Browser

    %% Step 2: Navigate to data migration
    Note over Browser,User: The migrated bucket (osId) is now available<br/>in the migrated object storage list.<br/>Select it as the destination in the data migration view.
```

---

## List migrated object storages

- Participants: Butterfly, Beetle, Tumblebug

> [!NOTE]
>
> - The user navigates to the migrated object storage list page to review all buckets created in the target cloud.
> - From this page, the user can inspect objects inside each bucket and initiate deletion.
> - Key flow:
>   - --> Query all migrated object storages in the namespace
>   - --> Display the list with bucket ID, name, connection, status, and creation date
>   - --> User selects a bucket to inspect its objects

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant Butterfly as "Butterfly"
    participant Beetle as "Beetle"
    participant Tumblebug as "Tumblebug"

    %% Step 1: List all migrated object storages
    User->>Browser: Navigate to migrated object storage list
    activate Browser
    Browser->>Butterfly: Request list of migrated object storages
    activate Butterfly
    Butterfly->>Beetle: API call GET /beetle/migration/middleware/ns/{nsId}/objectStorage
    activate Beetle
    Beetle->>Tumblebug: API call GET /tumblebug/ns/{nsId}/resources/objectStorage
    activate Tumblebug
    Tumblebug-->>Beetle: Return object storage list
    deactivate Tumblebug
    Beetle-->>Butterfly: Return object storage list (MigratedObjectStorageListResponse)
    deactivate Beetle
    Butterfly-->>Browser: Respond with the list
    deactivate Butterfly
    Browser-->>User: Display migrated object storage list
    Note over Browser,User: Displays per bucket:<br/>id (osId), name, connectionName,<br/>status (e.g., "Available"), creationDate, description<br/>[Delete] button per row
    deactivate Browser

    %% Step 2: View detail of a specific object storage (optional)
    opt View object storage detail
        User->>Browser: Click on a bucket name or ID in the list
        activate Browser
        Browser->>Butterfly: Request detail of the selected object storage
        activate Butterfly
        Butterfly->>Beetle: API call GET /beetle/migration/middleware/ns/{nsId}/objectStorage/{osId}
        activate Beetle
        Beetle->>Tumblebug: API call GET /tumblebug/ns/{nsId}/resources/objectStorage/{osId}
        activate Tumblebug
        Tumblebug-->>Beetle: Return bucket detail (ObjectStorageInfo)
        deactivate Tumblebug
        Beetle-->>Butterfly: Return bucket detail (MigratedObjectStorageInfo)
        deactivate Beetle
        Butterfly-->>Browser: Respond with bucket detail
        deactivate Butterfly
        Browser-->>User: Display bucket detail page
        Note over Browser,User: Displays:<br/>id (osId), name, connectionName,<br/>status, creationDate, description
        deactivate Browser
    end

    %% Step 3: Inspect objects in a bucket (optional)
    opt Inspect objects in a bucket
        User->>Browser: Select a bucket and click "View Objects"
        activate Browser
        Browser->>Butterfly: Request object list for the selected bucket
        activate Butterfly
        Butterfly->>Beetle: API call GET /beetle/migration/middleware/ns/{nsId}/objectStorage/{osId}/object
        activate Beetle
        Beetle->>Tumblebug: API call GET /tumblebug/ns/{nsId}/resources/objectStorage/{osId}/object
        activate Tumblebug
        Tumblebug-->>Beetle: Return object list
        deactivate Tumblebug
        Beetle-->>Butterfly: Return object list (StorageObjectListResponse)
        deactivate Beetle
        Butterfly-->>Browser: Respond with object list
        deactivate Butterfly
        Browser-->>User: Display object list
        Note over Browser,User: Displays per object:<br/>key (path), size (bytes), lastModified,<br/>storageClass, eTag
        deactivate Browser
    end
```

---

## Delete migrated object storage

- Participants: Butterfly, Beetle, Tumblebug

> [!NOTE]
>
> - The user initiates deletion from the migrated object storage list page.
> - To prevent accidental deletion, the portal requires the user to confirm by typing either `delete` or the exact bucket name before the delete request is sent.
> - Key flow:
>   - --> User clicks the [Delete] button for a bucket in the list
>   - --> Portal displays a confirmation dialog
>   - --> User types `delete` or the exact bucket name to confirm
>   - --> Portal validates the input and sends the delete request to Beetle
>   - --> Beetle removes the bucket via Tumblebug and returns the result
>   - --> Portal refreshes the object storage list

```mermaid
sequenceDiagram
    participant User as "User"
    participant Browser as "Web Console"
    participant Butterfly as "Butterfly"
    participant Beetle as "Beetle"
    participant Tumblebug as "Tumblebug"

    %% Step 1: User clicks [Delete] on a bucket row
    User->>Browser: Click [Delete] on a bucket row (osId, name)
    activate Browser
    Browser-->>User: Display confirmation dialog
    Note over Browser,User: Dialog message:<br/>"To confirm deletion, type 'delete'<br/>or the bucket name: {bucketName}"<br/>[Input field] [Cancel] [Confirm]
    deactivate Browser

    %% Step 2: User enters confirmation input
    User->>Browser: Type 'delete' or exact bucket name and click [Confirm]

    alt Input does not match
        activate Browser
        Browser-->>User: Show validation error — "Input does not match. Please try again."
        deactivate Browser
    else Input matches
        activate Browser
        %% Step 3: Send delete request
        Browser->>Butterfly: Request to delete object storage (osId)
        activate Butterfly
        Butterfly->>Beetle: API call DELETE /beetle/migration/middleware/ns/{nsId}/objectStorage/{osId}
        activate Beetle
        Beetle->>Tumblebug: API call DELETE /tumblebug/ns/{nsId}/resources/objectStorage/{osId}
        activate Tumblebug
        Tumblebug-->>Beetle: Return deletion result
        deactivate Tumblebug
        Beetle-->>Butterfly: Return deletion result
        deactivate Beetle
        Butterfly-->>Browser: Respond with deletion confirmation
        deactivate Butterfly

        %% Step 4: Refresh the list
        Browser->>Butterfly: Request updated object storage list
        activate Butterfly
        Butterfly->>Beetle: API call GET /beetle/migration/middleware/ns/{nsId}/objectStorage
        activate Beetle
        Beetle->>Tumblebug: API call GET /tumblebug/ns/{nsId}/resources/objectStorage
        activate Tumblebug
        Tumblebug-->>Beetle: Return updated list
        deactivate Tumblebug
        Beetle-->>Butterfly: Return updated list
        deactivate Beetle
        Butterfly-->>Browser: Respond with updated list
        deactivate Butterfly
        Browser-->>User: Display updated object storage list
        deactivate Browser
    end
```
